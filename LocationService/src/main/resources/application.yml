spring:
  application:
    name: location
  datasource:
    url: jdbc:h2:mem:testdb;Mode=PostgreSQL
    platform: h2
  jpa:
    hibernate:
      ddl-auto: create
eureka:
  client:
    service-url:
      default-zone: ${EUREKA_HOME}
server:
  port: 8040